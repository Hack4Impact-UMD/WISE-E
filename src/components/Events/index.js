import React from 'react';
import { Container } from '@material-ui/core';

const Events = () => {
    return (
        <Container>
            <h1>Events</h1>
        </Container>
    );
}

export default Events;